events {
    worker_connections 2048;
}

http {
    keepalive_timeout 3600;
    proxy_read_timeout 3600;
    proxy_send_timeout 3600;

    server {
        listen 8080;
        listen [::]:8080;

        # ==========================
        # # VPS 1 - VLESS
        # ==========================
        location /vps1/vless-ws {
            proxy_pass http://217.217.225.14:10001;
            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_buffering off;
            proxy_request_buffering off;
            tcp_nodelay on;
        }

        # ==========================
        # # VPS 1 - Trojan
        # ==========================
        location /trojan-ws {
            proxy_pass http://217.217.225.14:10002;
            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_buffering off;
            proxy_request_buffering off;
            tcp_nodelay on;
        }

        # ==========================
        # # VPS 1 - VMess
        # ==========================
        location /vmess-ws {
            proxy_pass http://217.217.225.14:10003;
            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_buffering off;
            proxy_request_buffering off;
            tcp_nodelay on;
        }

        # ==========================
        # # VPS 1 - Shadowsocks
        # ==========================
        location /shadow-ws {
            proxy_pass http://217.217.225.14:10004;
            proxy_http_version 1.1;

            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

            proxy_buffering off;
            proxy_request_buffering off;
            tcp_nodelay on;
        }
    }
}
